Base white surface — 1px border, radius-lg, elevation-1 at rest. Compose page content from it.

```jsx
<Card elevation={1} padding={20}>…</Card>
<Card interactive elevation={2}>…</Card>
```

Props: `elevation` 0–3, `radius`, `interactive`, `padding`.
