Dropdown select. Closed shows the current value; open lists options with the active one checked on a green-tinted row.

```jsx
<Select label="Etapa" value={stage} onChange={setStage}
  options={[{value:'new',label:'Nuevo'},{value:'contacted',label:'Contactado'}]} />
```
