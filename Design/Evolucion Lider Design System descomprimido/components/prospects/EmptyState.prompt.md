Centered empty state — icon in a green circle, title, support text, primary CTA. Shown when the user has no prospects yet.

```jsx
<EmptyState icon="users" title="Aún no tienes prospectos"
  description="Añade tu primer prospecto para empezar a construir tu red."
  ctaLabel="Añadir prospecto" onCta={…} />
```
